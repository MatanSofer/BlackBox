package com.blackbox.domain.model.place

/**
 * Category classification for known places.
 *
 * Used for grouping, display icons, and default naming
 * of auto-detected places.
 */
enum class PlaceCategory {
    HOME,
    WORK,
    GYM,
    RESTAURANT,
    SHOPPING,
    SCHOOL,
    MEDICAL,
    ENTERTAINMENT,
    TRANSIT,
    OTHER,
}
